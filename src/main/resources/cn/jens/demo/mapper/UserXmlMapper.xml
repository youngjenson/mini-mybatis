<?xml version="1.0" encoding="UTF-8" ?>
<mapper namespace="cn.jens.demo.mapper.UserXmlMapper">
    <cache/>

    <resultMap id="userResultMap" type="cn.jens.demo.entity.User">
        <id property="id" column="user_id"/>
        <result property="name" column="user_name"/>
        <result property="age" column="user_age"/>
        <result property="email" column="user_email"
                javaType="cn.jens.demo.type.EmailAddress" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectList" resultMap="userResultMap">
        select id as user_id, name as user_name, age as user_age
        from user
        order by id
    </select>

    <select id="selectById" resultMap="userResultMap">
        select id as user_id, name as user_name, age as user_age
        from user
        where id = #{id}
    </select>

    <select id="selectFreshById" resultMap="userResultMap" flushCache="true">
        select id as user_id, name as user_name, age as user_age
        from user
        where id = #{id}
    </select>

    <select id="selectWithoutCacheById" resultMap="userResultMap" useCache="false">
        select id as user_id, name as user_name, age as user_age
        from user
        where id = #{id}
    </select>

    <select id="selectDynamic" resultMap="userResultMap">
        select id as user_id, name as user_name, age as user_age
        from user
        <where>
            <if test="name != null and name != ''">
                name = #{name}
            </if>
            <if test="minAge != null">
                AND age &gt;= #{minAge}
            </if>
        </where>
        order by id
    </select>

    <select id="selectByIds" resultMap="userResultMap">
        select id as user_id, name as user_name, age as user_age
        from user
        <where>
            <if test="ids == null or ids.size == 0">
                1 = 0
            </if>
            <foreach collection="ids" item="id" nullable="true"
                     open="id in (" separator="," close=")">
                #{id}
            </foreach>
        </where>
        order by id
    </select>

    <select id="count" resultType="integer">
        select count(*) from user
    </select>

    <select id="selectWithEmailById" resultMap="userResultMap">
        select id as user_id, name as user_name, age as user_age, email as user_email
        from user
        where id = #{id, javaType=integer, jdbcType=INTEGER}
    </select>

    <insert id="insert">
        insert into user (id, name, age)
        values (#{id}, #{name}, #{age})
    </insert>

    <insert id="insertWithEmail">
        insert into user (id, name, age, email)
        values (#{id}, #{name}, #{age},
                #{email, javaType=cn.jens.demo.type.EmailAddress, jdbcType=VARCHAR})
    </insert>

    <update id="update">
        update user
        set name = #{name}, age = #{age}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>
</mapper>
